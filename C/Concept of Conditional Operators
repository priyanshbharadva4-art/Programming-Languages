#include<stdio.h>
int main()
{
    int x, y, max;

    printf("Enter two numbers: ");
    scanf("%d %d", &x, &y);

    max = (x > y) ? x : y;
    printf("The maximum of %d and %d is: %d\n", x, y, max);
    return 0;
}