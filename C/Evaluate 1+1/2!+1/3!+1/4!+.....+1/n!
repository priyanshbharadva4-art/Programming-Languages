#include<stdio.h>
int main()
{
    float i, n, sum, fact = 1;
    
    printf("Enter the value of n: ");
    scanf("%f", &n);

    for(i = 1; i <= n; i++)
    {
        fact *= i;
        sum = 1+ 1 / fact;
    }
    printf("The sum is %f", sum);
    return 0;
}