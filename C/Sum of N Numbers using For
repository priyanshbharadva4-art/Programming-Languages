#include<stdio.h>
int main()
{
    int n, i = 0, sum = 0;
    printf("Enter the value of N: ");
    scanf("%d", &n);

    for (i = 0; i <= n; i++)
    {
        sum += i;
    }
    printf("Sum of first %d numbers is: %d", n, sum);
    return 0;
}
