#include<stdio.h>
int main()
{
    double n, i = 1, sum = 0;
    printf("Enter the value of N: ");
    scanf("%lf", &n);

    do
    {
        sum += i;
        i++;
    } while(i <= n);
    printf("Sum of first %.0f numbers is: %.0f", n, sum);
    return 0;
}